# Supabase (form storage)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Hostinger SMTP (form notification emails)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=info@urbanfleetdelivery.ae
SMTP_PASS=
# Where application/inquiry emails are delivered (defaults to SMTP_USER if unset)
MAIL_TO=info@urbanfleetdelivery.ae
